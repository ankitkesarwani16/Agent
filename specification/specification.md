# Specification

> **Guidelines**: Read [guidelines.md](./guidelines.md) before executing ANY tasks below.

Check off items as completed.

## Solution Setup

- [x] Create asset directory: `mkdir -p assets/conversation-reply-agent/`
- [x] Invoke `setup-solution` skill to create `solution.yaml` and `asset.yaml` files for the `conversation-reply-agent` asset
- [x] Validate `assets/conversation-reply-agent/asset.yaml` and `solution.yaml` exist and are well-formed

## Asset Implementation

- [x] Execute specification/conversation-reply-agent/specification.md (all items)
